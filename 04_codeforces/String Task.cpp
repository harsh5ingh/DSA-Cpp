#include<iostream>
#include<cctype>
using namespace std;


int main(){
  string s;
  cin >> s;
  for (int i = 0; i < s.length(); i++)
  {
    char c = tolower(s[i]);
     result = [a,e,i,o,u];
    if (result += c)
    {
      /* code */
    }
    
  }

}